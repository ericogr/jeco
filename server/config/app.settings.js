'use strict';

//app parameters
module.exports = {
	'MAX_RANKING_RECORDS': 30, //max ranking records to store
	'GAME_RESET_DAYS': 365, //reset every xx days
	'RANKING_HISTORY_TASK_CHECK_INTERVAL': 3600000 //interval to execute a new game creation
};